# FastAPI Backend Environment Template

# App Settings
# Set DEVELOPMENT_MODE=false for every hosted environment.
DEVELOPMENT_MODE=true
# Hosted deployments: set this to the exact browser origin(s), comma-separated.
# Do not use * and do not include paths. A trailing slash is accepted and
# normalised, but it is clearer to omit it.
CORS_ORIGINS="http://localhost:3000,http://127.0.0.1:3000"

# PostgreSQL / Supabase
# If deploying on Railway, DATABASE_URL is automatically populated.
DATABASE_URL="postgresql://postgres:your_password@your_pooler_host:5432/postgres"
SUPABASE_URL="https://your-project.supabase.co"
SUPABASE_ANON_KEY="your_supabase_anon_key"
SUPABASE_SERVICE_ROLE_KEY="your_supabase_service_role_key"
SUPABASE_JWT_SECRET="your_supabase_jwt_secret"

# Gemini Live / Gemini embeddings (server-only)
GEMINI_API_KEY="your_gemini_api_key"
GEMINI_LIVE_MODEL="gemini-3.1-flash-live-preview"
GEMINI_EMBEDDING_MODEL="gemini-embedding-001"

# Groq persona generation and transcription (server-only)
GROQ_API_KEY="your_groq_api_key"
GROQ_PERSONA_MODEL="llama-3.3-70b-versatile"

# Pinecone Vector Database
PINECONE_API_KEY="your_pinecone_api_key"
PINECONE_INDEX="emmy-memories"
PINECONE_INDEX_NAME="emmy-memories"
PINECONE_ENVIRONMENT="us-east-1"
