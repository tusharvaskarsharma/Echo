"use client";
import { useState } from "react"; import { createClient } from "@/lib/supabase/client";
export default function ForgotPassword() { const [email,setEmail]=useState(""),[message,setMessage]=useState(""); async function submit(e:React.FormEvent){e.preventDefault(); await createClient().auth.resetPasswordForEmail(email,{redirectTo:`${location.origin}/reset-password`}); setMessage("If this address has an account, a reset link has been sent.");} return <main className="min-h-screen flex items-center justify-center p-6"><form onSubmit={submit} className="clay-card p-8 w-full max-w-md space-y-5"><h1 className="text-4xl font-serif text-primary">Reset password</h1><p>{message}</p><input className="w-full p-3" type="email" required placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><button className="clay-button-primary w-full p-3">Email reset link</button></form></main>; }
